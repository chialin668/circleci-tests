machine:
    timezone:
        America/Los_Angeles
    python:
        version: 2.7.10

dependencies:
  pre:
    - pip install emulambda

test:
  override:
    - cd emulambda; emulambda -v test1.py event.json
